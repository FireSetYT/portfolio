# 1. Використовуємо Node.js (версія 18)
FROM node:18

# 2. Створюємо папку всередині контейнера
WORKDIR /app

# 3. Копіюємо файли налаштувань
COPY package*.json ./

# 4. Встановлюємо бібліотеки (Express, Cors)
RUN npm install

# 5. Копіюємо весь проєкт у контейнер
COPY . .

# 6. Відкриваємо порт (для інформації)
EXPOSE 3000

# 7. Запускаємо сервер
CMD ["node", "server.js"]